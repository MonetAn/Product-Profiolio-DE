# Иконки админки

- **`paw-print-bold.svg`** — жирный силуэт лапы с [SVG Repo](https://www.svgrepo.com/) (раздел cat paw). Дубликат path встроен в `AdminScenarioIllustrations.tsx`; перед коммерческим использованием **проверь лицензию на странице конкретного файла** на svgrepo.com.
- Следы «Заполни по шагам» — градиент через CSS-переменные в `index.css`. Раньше пробовали `cat-paw-silhouette.png` как `mask-image`, но файл оказался **JPEG без альфа** — не повторять без настоящего PNG с прозрачностью.
