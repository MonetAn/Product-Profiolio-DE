# Правая карточка — сетка таблицы

**Текущее:** сетка **5×3** (15 ячеек), контейнер чуть шире (`w-[9.25rem]` / `sm:w-[11.5rem]`), `gap-2` — ячейки крупнее, чем старый прод 4×3. Hover: задержка **90 ms × строка + 42 ms × столбец** (волна чтения), easing чуть мягче, лёгкий **scale(1.04)**. Reduced motion: без scale и без анимации задержек.
